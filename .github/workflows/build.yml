name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: debian-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install live build
      run: apt install live-build

    - name: Build ISO image
      run: lb build
